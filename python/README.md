## Usage

若更新了新函数，需要先执行 generate_init.py
pip3 install . --upgrade

1. ubuntu需要安装python3-pytest  
2. 需要安装pip模块：pytest-mock, pytest-asyncio. 
3. 执行测试：python3 -m pytest .   

## 版本更新

执行python -m build进行包的构建

