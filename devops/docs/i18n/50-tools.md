# Tools

- `compile_po_tree`: compile PO tree to MO, preferring `msgfmt` and falling back to pure Python.
- `check_po_tree`: validate placeholders, fuzzy flags, empties, and duplicate keys for CI.
- CLI entry under `python -m mental1104.common.i18n.tools <cmd>`.
