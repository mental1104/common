# rust/.cargo/config.toml

# 仅在 x86_64 主机上启用 x86-64-v3 优化；其余架构保持默认以避免
# 在 arm64 (Apple Silicon 等) 上出现 “not a recognized processor” 的警告。
[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64-v3"]

[term]
verbose = false
